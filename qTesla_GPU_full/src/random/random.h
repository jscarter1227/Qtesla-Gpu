#ifndef __RANDOM_H__
#define __RANDOM_H__

// Generate random bytes and output the result to random_array
__device__ __host__ void randombytes(unsigned char* random_array, unsigned int nbytes);

#endif
